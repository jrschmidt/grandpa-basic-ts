// Generated by CoffeeScript 1.6.3
var ConsoleLine, Gb80Console;

Gb80Console = (function() {
  function Gb80Console() {
    this.line = new ConsoleLine;
    this.reset();
  }

  Gb80Console.prototype.reset = function() {
    this.status = "clean";
    return this.lines = [];
  };

  Gb80Console.prototype.addline = function(line) {
    return this.lines.push(line);
  };

  Gb80Console.prototype.fetch = function(n) {
    if (n >= 0 && n < this.lines.length) {
      return this.lines[n];
    } else {
      return "";
    }
  };

  return Gb80Console;

})();

ConsoleLine = (function() {
  function ConsoleLine() {
    this.text = "";
  }

  ConsoleLine.prototype.set_text = function(string) {
    return this.text = string;
  };

  ConsoleLine.prototype.get_text = function() {
    return this.text;
  };

  return ConsoleLine;

})();
